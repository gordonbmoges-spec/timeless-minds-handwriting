{
  "name": "timeless-minds-handwriting",
  "version": "0.1.0",
  "description": "A handwriting-first historical persona notebook for web and iPad.",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gordonbmoges-spec/timeless-minds-handwriting.git"
  },
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "node build.mjs",
    "check": "node --check server.js && node --check public/app.js",
    "security:secrets": "node scripts/check-secrets.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
