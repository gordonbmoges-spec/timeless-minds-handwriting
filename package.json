{
  "name": "ink-diary-web-mvp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "node build.mjs",
    "check": "node --check server.js && node --check public/app.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  }
}
